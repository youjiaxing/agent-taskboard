[package]
name = "host-kernel"
version = "0.1.0"
edition = "2021"
description = "Agent Taskboard Host 内核"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
