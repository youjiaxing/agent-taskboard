# Agent Taskboard v1 验收记录（Issue #90）

- 撰写时刻：2026-08-24；同日修订一次（反映主流程随后完成的修复与本轮复跑结果）
- 分支：`feat/90-v1-acceptance`；基线提交：`af235cd`（Merge PR #93）
- 验收对象：[#45 Agent Taskboard v1 产品规格](https://github.com/youjiaxing/agent-taskboard/issues/45)（156 条用户故事）
- 本文件只做客观记录与规格对照，不替代 #45 的收尾判断；本票（#90）不修改、不关闭 #45。

## 0. 基线事实

- 基线提交 `af235cd`：`Merge pull request #93 from youjiaxing/wip/88-project-registration-boundary`，即 #87、#88、#89 三张实现票合并后的主树状态。
- #87「补齐 GitHub Tracker 完整能力与数据可靠性」：**已关闭**（2026-08-24 05:27）。
- #88「完善 Project 登记、远程路径和本机数据边界」：**已关闭**（2026-08-24 13:21）。
- #89「补齐 Host 启动模式与启动环境管理」：**已关闭**（2026-08-24 07:09）。
- #90 本身：**OPEN**（标签 ready-for-agent），原生无阻塞链接——`issues/90/timeline` 中无任何 `cross-referenced` 事件，即原生 blocker = 0（正文中的「Blocked by #87–#89」只是描述性文字）。
- 三张前置票的合并都已在基线内，本票验收无需再等它们。

## 1. 验收矩阵（1–156，按验收域分组，逐项编号）

状态取值：**通过**（内核/合同层有测试证据或已由本票核验）、**部分**（实现存在但覆盖不完整或仅壳层证据）、**待真实平台**（必须真机/真实服务/真实 CI 才能确认）。证据列给出测试名或文件，测试位于 `crates/host-kernel/tests/`（除非另注）。矩阵正文按域拆在 `v1/` 分册，避免单文件过重。

| 分册 | 域 | 编号 |
|---|---|---|
| [安装、配对与回环](v1/host-pairing.md) | A 安装/Host 常驻；B 配对/网络/回环 | 1–8、16、17；9–15 |
| [Project 与 Tracker](v1/projects-tracker.md) | C Project 登记；D Tracker/凭据 | 18–28；29–33 |
| [看板、主壳与 skills](v1/board-shell.md) | E 四列看板；F 主壳布局；G skills/执行已停 | 34–43；44–55；56–58 |
| [启动、Run、终端、改动与自动推进](v1/runs.md) | H 启动表单；I Run 生命周期；J Embedded Terminal；K 查看改动；L 自动推进 | 59–77；78–86；87–91；92–98；99–109 |
| [刷新、用量、Client 与发布](v1/refresh-client.md) | M 刷新/离线；N 用量；O 列表动作；P 语言主题；Q 发布更新；R 手机；S 内置 Agent；T 数据存放 | 110–124；125–132；133–138；139–143；144–146；147–150；151–154；155–156 |

## 2. 门的执行情况（按实际输出）

| 门 | 命令 | 结果 |
|---|---|---|
| Workspace 全量测试 | `cargo test --workspace --all-targets`（workspace = host-kernel + src-tauri） | **最新：238 通过 / 0 失败**（逐个 test binary 统计：advance 9、antigravity 5、board 18（含浏览器 e2e）、bound_runs 17、claude 5、codex 5、github 22、grok 10、host_kernel 35、isolation 11、launch_env 3、launch_form 14、projects 19、refresh 17、runs 14、tracker_seam 8、usage 8、view_changes 7、waiting 10、src-tauri startup 单元 1；doc-tests 0）。日志插件配置由 `cargo check`、完整桌面构建和源码配置复核验证。 |
| 浏览器 e2e | `host-kernel/tests/board.rs::browser_renders_incomplete_state_then_recovers_all_board_flows` → `node apps/desktop/e2e/board.mjs`（Playwright chromium 无头） | **通过**（含在 board 的 18 项内，最新一轮全量复跑一并通过）。此前的竞态已修复：断言由单次 `snapshot` 改为**有界轮询等待内核快照变为 false**（`board.mjs` 358–365 行）；内核级同语义断言（`launch_form.rs::prepare_form_uses_cli_seed_concrete_values`）亦通过。 |
| 前端 build/typecheck | `npm --prefix apps/desktop run build`（tsc && vite build） | 通过（vite 6.4.3，`built in 454ms`）；后续 `tauri build` 的前端重构建亦成功。 |
| `cargo check`（workspace） | `cargo check` | 通过（`Finished dev profile`，无错误）。 |
| Release contract | `npm --prefix apps/desktop run verify:release`（RELEASE_TAG=v0.1.0） | 通过：`release contract ok for v0.1.0`（版本三处一致、bundle targets=dmg+nsis、createUpdaterArtifacts、ad-hoc 签名、updater pubkey 与 endpoints、三平台 CLI 包在 lockfile 中）。 |
| macOS 本机构建（Apple Silicon） | `tauri build --target aarch64-apple-darwin --bundles app,dmg`，本次以 `createUpdaterArtifacts=false`（本机无发布私钥）运行 | **通过**：`Agent Taskboard.app` 与 `Agent Taskboard_0.1.0_aarch64.dmg` 产出，命令 **exit 0**（产物：`target/aarch64-apple-darwin/release/bundle/{macos,dmg}/`）。发布侧签名闭环未验证，见 §4。 |
| Windows 安装冒烟（CI） | `.github/workflows/windows-package-smoke.yml`（PR 触发） | 见 §3：最近一次 **失败**（run 32732087886）；lockfile 修复后无新 run，**需 PR CI 复跑**（当前主要待验证门）。 |

## 3. 本票发现并已修复的问题与发布事实

1. **Windows lockfile 缺失（run 32732087886）**：`Windows 安装冒烟`（分支 wip/88-project-registration-boundary，2026-08-24 13:20）在「构建 NSIS」步骤失败：`Error: Cannot find module '@tauri-apps/cli-win32-x64-msvc'`（npm 可选依赖在 lockfile 缺失时的已知问题）。**本分支修复**：重新生成 `apps/desktop/package-lock.json`（三平台 CLI 可选包全量入锁，含 `@tauri-apps/cli-win32-x64-msvc`），并给 `apps/desktop/scripts/verify-release.mjs` 增加回归断言——lockfile 必须包含 `@tauri-apps/cli-darwin-arm64`、`cli-darwin-x64`、`cli-win32-x64-msvc`。`verify:release` 已带新断言通过。
2. **v0.1.0 已发布（三平台资产）**：`发布桌面应用` workflow（run 32650050149，2026-08-23）发布 `v0.1.0`，资产：`Agent.Taskboard_0.1.0_darwin_aarch64.dmg`（5,669,913 B）、`Agent.Taskboard_0.1.0_darwin_x64.dmg`（6,077,364 B）、`Agent.Taskboard_0.1.0_windows_x64-setup.exe`（4,036,767 B），各配 `.app.tar.gz`/`.sig`/`latest.json`。README 已注明 ad-hoc 签名与不公证。
3. **主流程随后完成项**：① e2e 的 commandPreview 断言改为有界轮询并复跑通过（连同 #74 的 `main.ts` 局部更新自定义意图状态修复）；② 日志 5×5MB 轮转落地（`tauri-plugin-log 2.9.0`，见 §4）；③ macOS Apple Silicon 本机构建以 `createUpdaterArtifacts=false` 成功产出 app+dmg（exit 0）。

## 4. 当前未验证 / 部分项（不得以「测试通过」替代）

- **Windows 安装冒烟**：run 32732087886 失败；本分支 lockfile 修复后**无新 PR CI run**，须等 PR CI 复跑并绿（含 `npm ci`+NSIS 构建+静默安装+打开空壳+10529 就绪）。在 Windows CI 复跑通过前，Windows 侧安装交付态未确认。
- **macOS 本机 app+dmg（发布签名闭环）**：macOS Apple Silicon 本机构建已成功——以 `createUpdaterArtifacts=false`（本机无发布私钥）运行 `tauri build --target aarch64-apple-darwin --bundles app,dmg`，`Agent Taskboard.app` 与 `*_aarch64.dmg` 产出、**命令 exit 0**。未验证的是**发布侧签名闭环**：真实发布私钥签署 updater 产物（`.sig`/`latest.json`）、Tauri updater 验签与真实下载安装，均未在本机/真机执行。
- **真实托盘 / 系统自启 / 更新安装 / 系统通知**：内核与壳层逻辑有测试，但真实系统行为（托盘菜单点击、开机自启、下载-确认-安装-按原模式拉起、系统级通知横幅与声音）依赖真机人工验证，尚未执行。
- **日志 5×5MB 轮转**：已实现——`tauri-plugin-log 2.9.0` 已加入 `src-tauri`，配置 `max_file_size = 5 * 1024 * 1024`、`rotation_strategy = KeepSome(5)`（`src-tauri/src/lib.rs`），`cargo check` 与完整桌面构建通过并编译进该插件。仍如实说明：尚未人工写入超过 25MB 的日志来观察真实轮转行为（当前以插件配置与构建为证）。
- **GitLab / 本地 markdown**：只有 `TrackerSeam` 合同口子与 `TrackerKind::Github`，无实现——符合 #45「不要求 v1 同版完整」，但如作为交付承诺需另立项。
- **手机用量/遥测精简、主题快切、通知声音、端口占用提示等**：壳层行为，无自动化断言（见矩阵中「部分」项）。
- ~~e2e 断言竞态~~：已修复并复跑通过（见 §2），不再列为待办。

## 5. 结论（对 #45 的收尾判断）

- **#45 暂不可收尾**。剩余主要门：① 修复后的 **Windows PR 安装冒烟**（lockfile 修复后尚无新 run，须 PR CI 复跑并绿）；② **真实系统行为**（真实托盘交互、系统自启、更新安装全流程、系统通知与声音）待当前 PR / 真机人工验证；③ 矩阵中「部分」项（#20 桌面行尾菜单、#50 总览分组、#86 端口占用提示、#128/#129 展示层、#136 声音、#143 快切、#144–146 安装与更新、#149 手机精简）逐项确认或明确降级。已修复项（e2e 断言竞态、日志 5×5MB 轮转落地、macOS 本机构建）不再列为阻塞。
- 规格判断不因「238 项测试通过」而达成：测试主体为 Host 内核接口与 Adapter 合同测试（`tests/` 全组），壳层与真实平台行为仍有上述未验证项；本票属于「已验证内核 + 部分壳层 + 未验证真实平台」的验收状态。
- **本票（#90）不修改、不关闭 #45**，也不对任何用户故事改动规格；本文档仅记录证据与缺口。

## 6. 附：完整性校验

- 156 条用户故事编号 1–156 在分册矩阵中**恰好各出现一次**（分组见 A–T，索引 §1）；用 `grep -o '#[0-9]*'` 提取后与 `seq 1 156` 比对一致。
- 证据引用均为撰写时刻工作树中存在的测试名/文件名；门的结果均为实际命令输出（见 §2），未按预期数字填写。修订后已重新执行编号完整性检查（同上法），仍为 1–156 恰好各一次。
